import { useEffect, useState } from 'react';

export const Wallet = () => {
  const [wallet, setWallet] = useState(null);

  useEffect(() => {
    /**
     * Integration logic
     */
    console.log('Data');
  }, []);

  return null;
};
